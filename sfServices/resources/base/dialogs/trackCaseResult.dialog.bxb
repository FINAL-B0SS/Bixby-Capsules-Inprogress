dialog (Result) {
	match : CaseInfo (this)
	if (size(this) > 1) {
		template ("These are the cases I found in San Francisco") {
		}
	} else-if (size(this) == 1) {
		if (exists(this.serviceName)) {
			template ("#{value(this.serviceName)}") {
        speech ()
      }
		}
	} else{
		template ("No cases were found matching this request.")
	}
}