dialog (Progress) {
  match: CaseInfo
  choose (Random) {
    template (Fetching cases)
    template (Retrieving cases)
    template (Finding case)
    template (Searching cases)
    template (Pulling records)
  }
}
