dialog (Result) {
  match : CaseInfo (this)
  if ("size(this) > 1") {
    template ("These are the cases I found in San Francisco") {
    }
  } else {
    template ("No cases could be found for this request.")
  }
}