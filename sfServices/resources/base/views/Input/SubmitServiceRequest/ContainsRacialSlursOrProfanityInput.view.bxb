input-view {
  match {
    ContainsRacialSlursOrProfanity (this)
  }

  render {
    selection-of (this) {
      navigation-mode {
        read-many {
          page-size (size(this))
          page-content {
            underflow-statement (This is the first page)
            item-selection-question (Contains racial slurs or profanity?)
            overflow-statement (This is the last page)
            overflow-question (What would you like to do?)
          }
        }
      }
      select-button-text ("Select")
      where-each (this) {
        spoken-summary ("#{value(this.name)}.")
        cell-card {
          slot2 {
            content {
              primary ("#{value(this.name)}")
            }
          }
        }
      }
    }
  }
}