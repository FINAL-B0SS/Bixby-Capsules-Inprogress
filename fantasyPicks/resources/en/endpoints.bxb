endpoints {
  // authorization {
  //   none
  //   // or add OAuth configuration
  // }

  action-endpoints {
    // action-endpoint (GetPick) {
    //   accepted-inputs (players)
    //   local-endpoint (GetPick.js)
    // }

    action-endpoint (GetPick) {
      accepted-inputs (players)
      remote-endpoint ("https://cmmtechskills.com/apis/fantasy-picks") {
        method (POST)
      }
      // headers {
      //   header (Content-Type: application/json)
      // } 
    }
  }
}
