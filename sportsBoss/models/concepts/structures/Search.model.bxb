structure (Search) {
  description (Stores RSS feed source)
  property (text) {
    type (Text)
    min (Required) max (One)
  }
  property (url) {
    type (Url)
    min (Required) max (One)
  }
  property (tags) {
    type (Tags)
    min (Required) max (Many)
  }
  features {
    transient
  }
}