endpoints {
  action-endpoints {
    action-endpoint (GetServiceInfo) {
      accepted-inputs ()
      local-endpoint (GetServiceInfo.js)
    }
    action-endpoint (SubmitServiceRequest) {
      accepted-inputs (description, firstName, lastName, email, phone, object, natureOfRequest, howManyPeople, howManyTents, type, containsRacialSlursOrProfanity, wholeBlock, location, serviceInfo, signType, signStatus, poleStatus, poleType)
      local-endpoint (SubmitServiceRequest.js)
    }
    action-endpoint (TrackCase) {
      accepted-inputs (serviceRequestNumber)
      local-endpoint (TrackCase.js)
    }
    action-endpoint (BuildNatureOfRequest) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildNatureOfRequest.js)
    }
    action-endpoint (BuildObject) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildObject.js)
    }
    action-endpoint (BuildHowManyPeople) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildHowManyPeople.js)
    }
    action-endpoint (BuildHowManyTents) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildHowManyTents.js)
    }
    action-endpoint (BuildContainsRacialSlursOrProfanity) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildContainsRacialSlursOrProfanity.js)
    }
    action-endpoint (BuildWholeBlock) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildWholeBlock.js)
    }
    action-endpoint (BuildSignStatus) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildSignStatus.js)
    }
    action-endpoint (BuildSignType) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildSignType.js)
    }
    action-endpoint (BuildPoleStatus) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildPoleStatus.js)
    }
    action-endpoint (BuildType) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildType.js)
    }
    action-endpoint (BuildPoleType) {
      accepted-inputs (serviceInfo)
      local-endpoint (Builds/BuildPoleType.js)
    }
    action-endpoint (viewMyRequests) {
      accepted-inputs (serviceRequestId)
      local-endpoint (viewMyRequests.js)
    }
  }
}